# Создаём список с помощью литерала списков
my_list = [3.14, 'string', {1: 'one', 2: 'two'}]
# Обходим каждый элемент списка
for item in my_list:
    # Вывод в stdout
    print(f'{item} => {type(item)}')


# Written by PaulKorotenko
